apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: test-storefront-imagecache
spec:
  storageClassName: nfs-class
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Mi